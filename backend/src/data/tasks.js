let tasks = [];
let currentId = 1;

module.exports = { tasks, currentId };

//Me va servir para eliminar o actualizar tareas, ya que cada tarea tendrá un id único. 
// Además, el array tasks se utilizará para almacenar las tareas en memoria